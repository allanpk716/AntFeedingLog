# 票 05 · 收口：文案同步 + 全链冒烟 + 版本账目

Spec：`docs/superpowers/specs/20260918-backup-restore-logging-spec.md`（D11 收口 + Further Notes 自查）

## What to build

二期收口：

1. **帮助文案同步**：设置页"数据"页签帮助文案更新为新语义（自动备份/恢复/日志），清除"应用内不做导入恢复、恢复=手动拷回"旧口径；`system.rs` 模块头注释同步（ADR-0002 的遗留动作）。
2. **全链冒烟测试**（自动化，端到端）：造数据 → 自动备份产生备份文件 → 修改数据 → 从备份恢复 → 断言数据回到备份点、备份配置未变、日志流水完整记录了备份与恢复事件。
3. **Further Notes 自查清单落账**：网络盘锁外拷贝、原子改名占用边界、刷新降级、台账重发窗口、运行标记误报——逐项在代码/测试中确认处置或标注"开发期关注"留给用户。
4. **版本号**：三处版本号（package.json / tauri.conf.json / Cargo.toml）升至 0.3.0（发版由用户决定，本票只改号）。

## 验收标准

- [ ] 设置页数据页签无旧口径文案残留（全文搜索"不做导入恢复"仅剩历史文档命中）。
- [ ] 全链冒烟测试通过：备份→改数据→恢复→断言回点 + 配置不变 + 日志事件齐全。
- [ ] Further Notes 五项逐项有落账（测试覆盖或明确标注）。
- [ ] 三处版本号一致为 0.3.0，`node scripts/check-versions.mjs . v0.3.0` 自检通过。
- [ ] 全量 cargo test + 前端测试全绿。

## Blocked by

票 03（自动备份引擎），票 04（恢复）。
