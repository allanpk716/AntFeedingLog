# 票 01 · 应用外壳：顶栏固定 + 内容独立滚动 + 右键屏蔽 + 窗口 960×680

**What to build**: 桌面应用任何窗口高度下导航栏（首页/统计/记录）始终可见、只有内容区滚动；无多余常驻滚动条；右键不再弹浏览器菜单（输入框内保留）；新装用户默认窗口 960×680。（= 计划 rev1 Task 1，含复审修正：右键安装器为模块级真幂等，卸载器执行前校验自身身份。）

**验收标准**
- [ ] html/body/#app 高度 100%、margin 0；根容器 100vh flex 列，顶栏 flex:none，`.page-body` 唯一 overflow-y:auto 容器
- [ ] 各页容器底衬 80px→32px，内容不足一屏时不出现滚动条
- [ ] tauri.conf.json 窗口 960×680
- [ ] 普通元素右键被屏蔽、输入框/文本域/contenteditable 放行（单测覆盖）
- [ ] 右键安装器：重复安装返回同一卸载器；卸载→重装→调用陈旧卸载器不破坏模块态（复审修正，单测覆盖）
- [ ] `pnpm test` 全绿后提交

**Blocked by**: 无，可立即开始
