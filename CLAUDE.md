# AntFeedingLog — 开发约定

单用户本地 Tauri 应用（蚂蚁饲养记录，桌面端全功能 + 受信网段网页端日常记录）。
术语以 CONTEXT.md 词汇表为准（改术语先改它），架构决议在 docs/adr/，组件与同名 `*.test.ts` 同目录（Vitest）。

## 操作反馈规范（全局强制，所有写操作适用）

- **判定原则：操作完成后界面没有天然反馈的（窗不关、视图不变），必须给轻提示；有天然反馈的（窗关闭、列表即时变化）不弹。**
- 覆盖：设置保存、字典行级停用/启用/删除、安全备份、导出、清理孤儿照片等一切"改了不关窗"的写操作。新写操作照此判定，不另立标准。
- 形态：底部居中浮层；成功绿色约 2.5 秒自动消失；失败红色约 5 秒、文案带原因、可点 × 提前关；同屏至多 3 条堆叠。
- 例外：表单校验错误不进轻提示——维持字段旁内联红字；催办提醒（"该喂食了"）走系统通知渠道，永不进轻提示。
- 实现：全局唯一 toast 模块（store + 组件挂应用根，桌面端与网页端同享）；各处只调用，不得自造提示样式。
