# 蚂蚁饲养记录（AntFeedingLog）

单用户本地工具：为多窝蚂蚁登记喂食、换水、保湿、清理等日常维护操作，可视化饲养历史，并提示哪些操作该做了。

## Language

### 核心对象

**窝（Colony）**：
一窝独立饲养的蚂蚁，有名字、物种、地点、开始饲养日期和状态。所有记录都挂在窝上。
_Avoid_: 用物种指代窝（同一物种可能养多窝）

**物种（Species）**：
窝的属性，如大头收获蚁、针毛收获蚁。只作描述，不用作标识。

**地点（Location）**：
窝所在的饲养位置，如家、公司。窝的属性，可以修改。

**开始饲养日期（Start Date）**：
一窝蚂蚁开始被照顾的第一天；"饲养天数"从它算起。

### 维护操作

**维护操作（Care Action）**：
对一窝蚂蚁做的一次照料行为，属可扩展清单。预置五种：喂食、活动区换水、巢穴保湿、垃圾清理、撤食；预置项不可删除，可改名、可停用。
_Avoid_: 任务、待办（这些是"还没做"的概念，记录是"已做"）

**喂食（Feeding）**：
维护操作之一，记录时附带投喂的食物。

**食物（Food）**：
喂食时投喂的东西，属可扩展清单。预置：种子、干虾仁、面包虫；预置项不可删除，可改名、可停用。

**易腐（Perishable）**：
食物的属性：开启后该食物附带撤食间隔（按小时计、从喂食时刻起算——区别于按天算的建议间隔）。预置默认：面包虫、干虾仁易腐（24 小时），种子不易腐。

**活动区换水（Activity-Area Water Change）**：
更换活动区（觅食区）的饮水。

**巢穴保湿（Nest Hydration）**：
给巢穴区域的保湿介质补水或换水。各窝怎么保湿因窝而异（见「保湿方式」），节奏差异走每窝周期。

**保湿方式（Hydration Method）**：
窝的属性，只对巢穴保湿有意义：未设（默认）/ 手动加水 / 水塔。它不是提醒开关——提不提醒、多少天，仍由每窝周期说了算；它的作用是把饲养经验固化成默认周期：选「手动加水」预填 7 天、选「水塔」预填 15 天，且只在周期为空时预填，已有周期永不覆盖（新建与事后改方式同一条规则）。方式清回未设＝周期一并清空；只清天数＝保留标签、不提醒。
_Avoid_: 把它当水塔窝的"免提醒开关"（水塔也会没水，照样可以设周期；方式与提醒互不绑架）

**垃圾清理（Trash Cleanup）**：
清理垃圾区（废物堆）。打卡时可顺带撤食（规则见「撤食」）。

**撤食（Food Retrieval）**：
收走活动区里没吃完的易腐食物。性质固定「跟随喂食」，不参与提醒类/登记类切换；撤一次视为清空该窝全部待撤食物。打卡「垃圾清理」时可顺带记一次撤食：已逾期默认带上、未到期默认不带、无待撤不出此选项；两条记录同一时刻、事后各自独立删除；仅垃圾清理单向带动，补录以所选时刻是否晚于该窝最近一次易腐喂食判定。撤食停用时该联动一并关闭。
_Avoid_: 清理、收食（「垃圾清理」清的是垃圾区，撤食收的是没吃完的食物，不是一个动作——顺带联动是两个动作一次手势，不是合并成一个动作）

**记录（Log Entry）**：
一次已发生的维护操作：哪窝 + 哪种操作 + 发生时间 +（喂食时）食物 + 备注。一次喂食可以在一条记录里多选几种食物。补录是合法的：发生时间可以早于录入时间，两者分开算。

**打卡（Quick Log）**：
从首页登记一次刚发生的操作：经确认面板提交，面板默认今天、可改过去日期补录、可填备注，喂食另选食物；取消不产生任何记录。

**删除记录（Delete Log Entry）**：
事后从记录列表移除一条已发生的记录；两段确认，删除即永久。
_Avoid_: 撤销（"刚打卡就反悔"由打卡面板的取消覆盖，是另一个概念）

### 巢况

**巢况登记（Nest Check-in）**：
对一窝蚂蚁当前状态的一次阶段性快照：日期 + 蚁后数（可空）+ 工蚁数（可空）+ 若干巢况照片（可零张）+ 备注；换巢的那次带"换巢"标记。入巢时的第一条巢况登记就是这窝的基线。
_Avoid_: 维护操作（巢况是观察不是照料，永不产生提醒）；把数蚁口和拍照当两件事（它们是同一次观察的两面）

**巢况照片（Nest Photo）**：
挂在某次巢况登记上的照片，记录蚁巢当时的样子；入库前自动压缩、原图不留。文件存库外的照片文件夹，库内只存元数据。

### 状态与提醒

**窝状态（Colony Status）**：
活跃 / 冬眠 / 已结束，三选一。

**冬眠（Hibernation）**：
收获蚁冬季的休整期，期间停食。冬眠中的窝提醒静音，但记录照常可以打。
_Avoid_: 越冬（用户习惯说"冬眠"）

**冬眠期（Hibernation Period）**：
一段有边界的冬眠时间：开始日期 + 预计结束日期 + 实际结束日期（出眠时回填）。预计结束前默认 7 天发临近出眠提醒，当天发出眠提醒。一窝可以有多段冬眠期（一年一段）。

**出眠（Wake）**：
手动结束冬眠、窝回到活跃状态的动作，出眠时记下实际结束日期。
_Avoid_: 苏醒、开眠

**提醒类 / 登记类（Reminding / Log-only）**：
维护操作的性质，二选一，按操作类型在设置里切换，是没设每窝周期的窝的默认行为。提醒类有建议间隔，超期标红、可发通知（当前配置：喂食、垃圾清理）。登记类只记录、只显示"距上次"，永不催促（当前配置：活动区换水、巢穴保湿——水塔窝看水位就行，不需要周期）。撤食是例外：不属于任何一种性质，它的「该做」跟随易腐喂食派生，不设建议间隔。
_Avoid_: 把登记类做成"提醒但间隔设得很长"——单窝需要周期就给它设每窝周期，别动全局

**建议间隔（Suggested Interval）**：
提醒类操作可配置的天数，"距上次"超过它就标红提醒；登记类没有。喂食的建议间隔分两层：统一周期与食物周期。任何操作的建议间隔都可被某窝的每窝周期取代。

**距上次（Days Since Last）**：
某窝某种操作距今多少天没做；喂食还按食物分别算"距上次喂某食物"。提醒类据此标红；登记类只展示，不催促。

**统一周期（Unified Interval）**：
喂食操作整层的建议间隔：距上次任何喂食超过它，就提醒"该喂食了"。设了喂食每窝周期的窝不受它管，按自己的节奏走。

**食物周期（Food-specific Interval）**：
挂在每种食物上的建议间隔：距上次喂该种食物超过它，就提醒"该喂某食物了"。未设周期的食物只受统一周期（或该窝的每窝周期）管；与操作层周期任一超期即提醒。

**每窝周期（Per-colony Interval）**：
挂在窝上、按操作设的周期（天）。设了即提醒：该窝该操作按它算超期（标红、每日通知，做了为止），并取代该操作层的建议间隔——喂食即取代统一周期，食物周期照旧叠加。没设的窝沿用操作层的性质——登记类操作对没设周期的窝依旧永不催促。冬眠静音照吃；从未做过该操作的窝不提醒，第一次做了才开始算。撤食不适用。巢穴保湿的周期可由「保湿方式」预填默认值，规则见该词条。
_Avoid_: 静音、关提醒（没设周期不是"关掉了"，是"沿用操作层默认"）

**通知渠道（Notification Channel）**：
提醒送达用户的途径：桌面通知与手机推送，并行发送、互不依赖；未送达的手机推送由下一轮调度自动补发。一个总开关管全部渠道。

**轻提示（Toast）**：
应用内短时浮现的结果条，告知一次操作的成败（"已保存""备份失败：××"）：成功绿色约 2.5 秒自动消失，失败红色约 5 秒、带原因、可提前关，同屏至多 3 条。判定规则：操作完成后界面没有天然反馈的（窗不关、视图不变）必须给轻提示；有天然反馈的（窗关闭、列表即时变化）不弹。表单校验错误不进轻提示，维持字段旁内联红字。
_Avoid_: 与「通知渠道」混用——"该喂食了"这类催办走系统通知渠道，"存好了"这类操作结果走轻提示，两套永不通勤

**待撤食（Pending Food Retrieval）**：
一窝的派生状态：最近一次易腐喂食已超过撤食间隔、且其后没有撤食记录。每窝至多一个、覆盖全部易腐食物；再喂易腐食物按最新一次重算。逾期当天起每天提醒，直到撤食。不吃冬眠静音（发霉不等人）；补录历史易腐喂食会进入该状态，但不补发通知。
_Avoid_: 待办、任务（这是从喂食记录算出来的状态，不是存下来的一条活儿）

### 入口与访问

**网页端（Web Client）**：
同一份数据的浏览器入口，只在受信网段内开放；功能限于日常记录（首页、打卡、历史记录、冬眠操作），管理功能一律没有。
_Avoid_: 手机版（按网络与权限界定，不按设备；平板、别的电脑开浏览器进来也是网页端）

**桌面端（Desktop）**：
Windows 程序本体，全功能唯一入口；网页端没有的管理（字典、备份、恢复、导出、更新、设置）都在这里。

**受信网段（Trusted Segment）**：
本机网卡上枚举出来、被明确允许访问网页端的网段；在设置里维护，可多选。
_Avoid_: 信任设备（受信的是网段，不是某台设备）

**访问令牌（Access Token）**：
网页端的进门凭证：随机生成，可在设置里修改或重新生成，随完整访问地址一起复制传播。
_Avoid_: 密码（用户永远不需要输入它，它活在书签网址里）

### 数据安全

**备份（Backup）**：
把当前全部数据——SQLite 库与巢况照片——完整拷一份到别处，产出单一"数据包"文件；手动"安全备份"与自动备份产出同一种文件。旧版本的纯库备份文件仍可恢复，只是不含照片。
_Avoid_: 导出（导出指 CSV/JSON 归档带走，给人看的，不是拿来恢复的）

**自动备份（Auto Backup）**：
每天第一次产生新数据时自动备份一次；当天已备且之后无新数据则跳过。
_Avoid_: 定时备份（触发规则是"当日有没有新数据"，不是时钟到点）

**备份目录（Backup Directory）**：
自动备份的落盘位置，在设置里指定；未指定时自动备份不生效。

**备份保留（Retention）**：
备份目录里只留最近 N 份（默认 30，可改），超出自动删最旧。

**恢复（Restore）**：
用一份备份整体替换当前全部数据——替换，不是合并。恢复前自动快照当前数据、预览备份摘要（含是否带照片）、二次确认。
_Avoid_: 导入（听起来像"把数据并进来"；恢复是整体替换，旧数据全部消失）

### 统计

**饲养天数**：
今天减去开始饲养日期，展示在窝的卡片上。
